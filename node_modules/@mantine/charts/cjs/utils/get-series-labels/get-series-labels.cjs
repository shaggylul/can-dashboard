'use client';
'use strict';

function getSeriesLabels(series) {
  if (!series) {
    return {};
  }
  return series.reduce((acc, item) => {
    const matchFound = item.name.search(/\./);
    if (matchFound >= 0) {
      const key = item.name.substring(matchFound + 1);
      acc[key] = item.label;
      return acc;
    }
    acc[item.name] = item.label;
    return acc;
  }, {});
}

exports.getSeriesLabels = getSeriesLabels;
//# sourceMappingURL=get-series-labels.cjs.map
